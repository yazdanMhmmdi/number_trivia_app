name: number_trivia_clean_architecture_tdd
description: A new Flutter project.

publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=2.15.1 <3.0.0"

dependencies:
  bloc_test: ^9.0.2
  cupertino_icons: ^1.0.2
  dartz: ^0.10.1
  data_connection_checker_tv: ^0.3.5-nullsafety
  dio: ^4.0.4
  equatable: ^2.0.3
  flutter:
    sdk: flutter
  flutter_bloc: ^8.0.1
  flutter_hooks: ^0.18.2+1
  get_it: ^7.2.0
  http: ^0.13.4
  json_annotation: ^4.3.0
  retrofit: ^3.0.1+1
  shared_preferences: ^2.0.12

dev_dependencies:
  build_runner: ^2.1.8
  flutter_lints: ^1.0.0
  flutter_test:
    sdk: flutter
  json_serializable: ^6.0.0
  mockito: ^5.0.17
  retrofit_generator: ^3.0.1+1

flutter:
  uses-material-design: true
